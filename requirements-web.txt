# Optional web extra for treval-web (EV-W0 registry viewer). NOT required by the
# core library or CLI — `import treval` works without these. Install with:
#   pip install -r requirements-web.txt
# All permissive-licensed (MIT/BSD), per Charter §1.2.
fastapi>=0.115
uvicorn>=0.30
jinja2>=3.1
httpx>=0.27   # used by fastapi.testclient.TestClient (tests for /api/registry, /)
